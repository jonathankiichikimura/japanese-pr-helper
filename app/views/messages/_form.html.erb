<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, label: false, required: true, placeholder: "Ask a question" %>
  <%= f.input :file %>
  <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
<% end %>
